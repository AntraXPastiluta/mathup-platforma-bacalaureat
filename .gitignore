node_modules/
.cursor/
.claude/

# Documentație locală (licență / agent) — vezi și scholar-bac/.gitignore
ARHITECTURA-APLICATIE.md
CLAUDE.md
CONTEXT-STARTUP-MATHUP.md
TEHNOLOGII-SI-LOGICI.md
Lucrare-licenta/

# Legacy/temporary folders outside project roots
mathup/

# Supabase migrations — not public
backend/supabase/migrations/

# Backend — secrets & Supabase CLI cache (see backend/.gitignore)
backend/.temp/
backend/supabase/.temp/
backend/.env
backend/.env.*
.env.docker

# OS/editor noise
.DS_Store
Thumbs.db
